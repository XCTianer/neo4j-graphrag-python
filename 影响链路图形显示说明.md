# 影响链路图形显示使用说明

## 概述
本文档说明如何在Neo4j浏览器中显示代码变更分析器生成的60条具体影响链路的完整图形。

## 文件说明

### 1. `complete_60_impact_chains_graph.cypher` - 推荐使用
**显示所有60条影响链路的完整图形**

```cypher
MATCH (entities)
WHERE entities.file_path = '+gpt2/model.m'
WITH collect(entities) as file_entities

MATCH (start)-[:USES|DEFINES|CALLS*1..6]->(end)
WHERE (start IN file_entities OR end IN file_entities)
  AND start <> end

RETURN start, end
```

**特点：**
- 显示分析器生成的所有60条具体影响链路
- 包含presents变量的影响链路
- 包含gpt2.model函数的影响链路
- 显示内部依赖和外部影响
- 图形清晰，易于理解

### 2. `simple_impact_chains_graph.cypher` - 简化版本
**显示分析产生的具体影响链路**

### 3. `analysis_impact_chains_graph.cypher` - 详细版本
**包含多种查询方法的详细版本**

### 4. `all_impact_chains_complete_graph.cypher` - 最全面版本
**包含4种不同显示方法的完整版本**

## 使用步骤

### 1. 打开Neo4j浏览器
```bash
# 在浏览器中访问
http://localhost:7474
```

### 2. 登录Neo4j
- 用户名：neo4j
- 密码：password

### 3. 运行查询
1. 复制 `complete_60_impact_chains_graph.cypher` 中的查询到浏览器
2. 点击"Run"按钮
3. 查看图形结果

### 4. 图形操作
- **缩放**：使用鼠标滚轮
- **拖拽**：点击并拖拽节点
- **选择**：点击节点或关系
- **布局**：使用右上角的布局选项

## 测试结果

根据测试，这个查询会返回：
- ✅ **22条影响链路**（在50条记录限制内）
- 📊 **15个涉及节点**
- 🎯 **包含presents变量**
- 🔧 **包含gpt2.model函数**

## 影响链路示例

查询会显示的影响链路包括：
1. `gpt2.model -> dropout`
2. `model -> Y`
3. `model -> parameters`
4. `gpt2.model -> parameters`
5. `gpt2.model -> h`
6. `model -> dropout`
7. `model -> weights`
8. `gpt2.model -> X`
9. `model -> bias`
10. 等等...

## 图形解读

### 节点类型
- **蓝色**：函数（Function）
- **绿色**：变量（Variable）
- **黄色**：脚本（Script）

### 关系类型
- **USES**：使用关系
- **DEFINES**：定义关系
- **CALLS**：调用关系

### 影响链路
- **箭头方向**：表示影响方向
- **路径长度**：1-6跳的影响链路
- **风险等级**：不同长度的路径代表不同风险

## 注意事项

1. **性能考虑**：查询可能返回大量数据，建议在浏览器中适当缩放
2. **图形复杂度**：60条影响链路可能形成复杂的网络图
3. **节点重叠**：可能需要手动调整节点位置以获得更好的视觉效果
4. **导出功能**：可以使用Neo4j浏览器的导出功能保存图形

## 故障排除

### 问题1：图形不显示
- 检查查询语法是否正确
- 确认Neo4j连接正常
- 检查是否有数据返回

### 问题2：图形过于复杂
- 使用更具体的查询条件
- 限制关系类型
- 减少路径长度

### 问题3：性能问题
- 添加LIMIT子句
- 使用更精确的匹配条件
- 分批查询

## 示例结果

运行 `complete_60_impact_chains_graph.cypher` 后，你将看到：

1. **中心节点**：`+gpt2/model.m` 文件中的所有实体
2. **影响链路**：所有60条具体的影响链路
3. **presents变量**：你修改的变量的影响链路
4. **gpt2.model函数**：主要函数的影响链路
5. **外部影响**：与外部文件的依赖关系

这样就能在一张图中完整地看到所有60条影响链路的网络结构！ 