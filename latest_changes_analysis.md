# 最新变更记录详细分析

**分析时间**: 2025-06-22 14:23:07

**仓库**: https://github.com/XCTianer/transformer-models.git

**总提交数**: 2

## 提交 1: Update GPT2 support file path handling

- **提交哈希**: ce31873aa16567057267ec6a71dbd96eda302dd3
- **作者**: xiechitian
- **时间**: 2025-06-22 14:08:46 +0800
- **文件变更**: 1 个
- **新增行数**: 1 行
- **删除行数**: 1 行

### 变更文件

- `a/nj/transformer-models/+gpt2/+internal/getSupportFilePath.m`

### MATLAB代码变更

- **变量变更**: -fileURL, +fileURL

---

## 提交 2: Initial commit: Transformer models project

- **提交哈希**: 0b9d76271d16c02777f484d7cd6bd412fbe22f96
- **作者**: xiechitian
- **时间**: 2025-06-22 13:15:32 +0800
- **文件变更**: 97 个
- **新增行数**: 6999 行
- **删除行数**: 0 行

### 变更文件

- `a/nj/transformer-models/+bert/+internal/convertModelNameToDirectories.m`
- `a/nj/transformer-models/+bert/+internal/createParameterStruct.m`
- `a/nj/transformer-models/+bert/+internal/encodeWithMaskToken.m`
- `a/nj/transformer-models/+bert/+internal/getSupportFilePath.m`
- `a/nj/transformer-models/+bert/+internal/inferTypeID.m`
- `a/nj/transformer-models/+bert/+internal/predictMaskedToken.m`
- `a/nj/transformer-models/+bert/+layer/block.m`
- `a/nj/transformer-models/+bert/+layer/classifier.m`
- `a/nj/transformer-models/+bert/+layer/classifierHead.m`
- `a/nj/transformer-models/+bert/+layer/embedding.m`
- `a/nj/transformer-models/+bert/+layer/languageModelHead.m`
- `a/nj/transformer-models/+bert/+layer/pooler.m`
- `a/nj/transformer-models/+bert/+tokenizer/+internal/BasicTokenizer.m`
- `a/nj/transformer-models/+bert/+tokenizer/+internal/FullTokenizer.m`
- `a/nj/transformer-models/+bert/+tokenizer/+internal/TokenizedDocumentTokenizer.m`
- `a/nj/transformer-models/+bert/+tokenizer/+internal/Tokenizer.m`
- `a/nj/transformer-models/+bert/+tokenizer/+internal/WhitespaceTokenizer.m`
- `a/nj/transformer-models/+bert/+tokenizer/+internal/WordPieceTokenizer.m`
- `a/nj/transformer-models/+bert/+tokenizer/BERTTokenizer.m`
- `a/nj/transformer-models/+bert/languageModel.m`
- `a/nj/transformer-models/+bert/load.m`
- `a/nj/transformer-models/+bert/model.m`
- `a/nj/transformer-models/+finbert/+internal/convertModelNameToDirectories.m`
- `a/nj/transformer-models/+finbert/+internal/getSupportFilePath.m`
- `a/nj/transformer-models/+finbert/languageModel.m`
- `a/nj/transformer-models/+finbert/load.m`
- `a/nj/transformer-models/+finbert/model.m`
- `a/nj/transformer-models/+finbert/sentimentModel.m`
- `a/nj/transformer-models/+gpt2/+internal/getSupportFilePath.m`
- `a/nj/transformer-models/+gpt2/+layer/block.m`
- `a/nj/transformer-models/+gpt2/+tokenizer/GPT2Tokenizer.m`
- `a/nj/transformer-models/+gpt2/download.m`
- `a/nj/transformer-models/+gpt2/load.m`
- `a/nj/transformer-models/+gpt2/model.m`
- `a/nj/transformer-models/+sampling/sampleFromCategorical.m`
- `a/nj/transformer-models/+sampling/topKLogits.m`
- `a/nj/transformer-models/+transformer/+layer/attention.m`
- `a/nj/transformer-models/+transformer/+layer/convolution1d.m`
- `a/nj/transformer-models/+transformer/+layer/dropout.m`
- `a/nj/transformer-models/+transformer/+layer/gelu.m`
- `a/nj/transformer-models/+transformer/+layer/maskAttentionWeights.m`
- `a/nj/transformer-models/+transformer/+layer/multiLayerPerceptron.m`
- `a/nj/transformer-models/+transformer/+layer/multiheadAttention.m`
- `a/nj/transformer-models/+transformer/+layer/normalization.m`
- `a/nj/transformer-models/.gitignore`
- `a/nj/transformer-models/ClassifyTextDataUsingBERT.m`
- `a/nj/transformer-models/FineTuneBERT.m`
- `a/nj/transformer-models/FineTuneBERTJapanese.m`
- `a/nj/transformer-models/PredictMaskedTokensUsingBERT.m`
- `a/nj/transformer-models/PredictMaskedTokensUsingFinBERT.m`
- `a/nj/transformer-models/README.md`
- `a/nj/transformer-models/README_JP.md`
- `a/nj/transformer-models/SECURITY.md`
- `a/nj/transformer-models/SentimentAnalysisWithFinBERT.m`
- `a/nj/transformer-models/SummarizeTextUsingTransformersExample.m`
- `a/nj/transformer-models/bert.m`
- `a/nj/transformer-models/finbert.m`
- `a/nj/transformer-models/generateSummary.m`
- `a/nj/transformer-models/gpt2.m`
- `a/nj/transformer-models/license.txt`
- `a/nj/transformer-models/predictMaskedToken.m`
- `a/nj/transformer-models/test/bert/internal/tinferTypeID.m`
- `a/nj/transformer-models/test/bert/tlanguageModel.m`
- `a/nj/transformer-models/test/bert/tmodel.m`
- `a/nj/transformer-models/test/bert/tokenizer/internal/tBasicTokenizer.m`
- `a/nj/transformer-models/test/bert/tokenizer/internal/tFullTokenizer.m`
- `a/nj/transformer-models/test/bert/tokenizer/internal/tTokenizedDocumentTokenizer.m`
- `a/nj/transformer-models/test/bert/tokenizer/internal/tWhitespaceTokenizer.m`
- `a/nj/transformer-models/test/bert/tokenizer/internal/tWordPieceTokenizer.m`
- `a/nj/transformer-models/test/bert/tokenizer/tBERTTokenizer.m`
- `a/nj/transformer-models/test/bert/tokenizer/tBERTTokenizerForJP.m`
- `a/nj/transformer-models/test/finbert/tlanguageModel.m`
- `a/nj/transformer-models/test/finbert/tsentimentModel.m`
- `a/nj/transformer-models/test/gpt2/layer/tblock.m`
- `a/nj/transformer-models/test/gpt2/tdownload.m`
- `a/nj/transformer-models/test/gpt2/tload.m`
- `a/nj/transformer-models/test/gpt2/tmodel.m`
- `a/nj/transformer-models/test/gpt2/tokenizer/tGPT2Tokenizer.m`
- `a/nj/transformer-models/test/sampling/tsampleFromCategorical.m`
- `a/nj/transformer-models/test/sampling/ttopKLogits.m`
- `a/nj/transformer-models/test/tbert.m`
- `a/nj/transformer-models/test/tfinbert.m`
- `a/nj/transformer-models/test/tools/DownloadBERTFixture.m`
- `a/nj/transformer-models/test/tools/DownloadFinBERTFixture.m`
- `a/nj/transformer-models/test/tools/DownloadGPT2Fixture.m`
- `a/nj/transformer-models/test/tools/DownloadJPBERTFixture.m`
- `a/nj/transformer-models/test/tools/getRepoRoot.m`
- `a/nj/transformer-models/test/tpredictMaskedToken.m`
- `a/nj/transformer-models/test/transformer/layer/tattention.m`
- `a/nj/transformer-models/test/transformer/layer/tconvolution1d.m`
- `a/nj/transformer-models/test/transformer/layer/tdropout.m`
- `a/nj/transformer-models/test/transformer/layer/tgelu.m`
- `a/nj/transformer-models/test/transformer/layer/tmaskAttentionWeights.m`
- `a/nj/transformer-models/test/transformer/layer/tmultiLayerPerceptron.m`
- `a/nj/transformer-models/test/transformer/layer/tmultiheadAttention.m`
- `a/nj/transformer-models/test/transformer/layer/tnormalization.m`
- `a/nj/transformer-models/truncateSequences.m`

### MATLAB代码变更

- **变量变更**: +function dirpath, +modelName, +bertBaseLocation, +    bertBaseLocation, +dirpath, +function supportfileName, +    names, +    map, +supportfileName, +function names, +names, +function weightsStruct, +f, +for i, +    name, +    encoderLayerPrefix, +    embeddingLayerPrefix, +    poolingLayerPrefix, +    langModPrefix, +    nspPrefix, +    genericClassifierPrefix, +    weight, +        layerIndex, +        newLayerIndex, +        layerName, +        shortLayerName, +        paramName, +        attentionOrFeedforward, +        [subParamName,subsubParamName], +        weightsStruct.("encoder_layers").(shortLayerName).(attentionOrFeedforward).(subParamName).(subsubParamName), +            [subname,subsubname], +            weightsStruct.("embeddings").(subname).(subsubname), +            weightsStruct.("embeddings").(paramName), +        weightsStruct.("pooler").(paramName), +        [subname,subsubname], +        weightsStruct.("masked_LM").(subname).(subsubname), +            paramName, +            weight, +        weightsStruct.("sequence_relation").(paramName), +        weightsStruct.("classifier").(paramName), +function name, +function [name,subname], +        [name,subname], +function [subname,subsubname], +    [subname,subsubname], +    subname, +    subsubname, +subname, +subsubname, +function [x,untokenizedPieces,ismask], +[seqs,untokenizedPieces], +x, +maskCode, +ismask, +function [x,pieces], +pieces, +fulltok, +    tokens, +        x, +function filePath, +directory, +sd, +localFile, +if exist(localFile,'file')~, +fileURL, +filePath, +function types, +xsz, +types, +sepId, +    sepId, +cs, +obsNeedsType2, +type2positions, +types(type2positions), +function [toks,probs], +    k (1,1) double {mustBePositive,mustBeInteger}, +probs, +[~,idx], +toks, +function z, +%   Z, +  nvp.HiddenDropout (1,1) double {mustBeNonnegative, mustBeLessThanOrEqual(nvp.HiddenDropout,1)}, +  nvp.AttentionDropout (1,1) double {mustBeNonnegative, mustBeLessThanOrEqual(nvp.AttentionDropout,1)}, +  nvp.InputMask, +z, +layer_input, +Q_w, +Q_b, +K_w, +K_b, +V_w, +V_b, +weights.attn_c_attn_w_0, +weights.attn_c_attn_b_0, +weights.attn_c_proj_w_0, +weights.attn_c_proj_b_0, +hyperparameters.NumHeads, +int_w, +int_b, +out_w, +out_b, +weights.mlp_c_fc_w_0, +weights.mlp_c_fc_b_0, +weights.mlp_c_proj_w_0, +weights.mlp_c_proj_b_0, +ffn_out, +out_g, +function y, +y, +function  z, +%   as mdl.Parameters.Weights.embeddings where mdl, +wordEmbedding, +typeEmbedding, +positionEmbedding, +%   mdl.Parameters.Weights.masked_LM struct where mdl, +%   mdl, +    properties(SetAccess, +    properties(Constant,Access, +        WhiteSpaceTokenizer, +        function this, +                nvp.IgnoreCase (1,1) logical, +            this.IgnoreCase, +        function tokens, +            tokens, +            for i, +                thisText, +                u, +                    thisText, +                cats, +                    [u,cats], +                theseTokens, +                tokens{i}, +    methods(Access, +        function tok, +            tok, +        function u, +            udata, +            ucats, +            isWhitespace, +            isControl, +            udata(isWhitespace), +            udata(isControl), +            u.Data, +        function tf, +            whitespaceChar, +            whitespaceCat, +            tf, +            controlExceptions, +            controlChar, +            controlCat, +            isControlCat, +        function out, +            out, +            function u, +                u.Data, +                isPunc, +                    out{i}, +                    isBeforePunc, +                    isBeforePunc(end), +                    splitBoundary, +                    splitBoundary(end), +                    splitLens, +                    newdata, +                    newU, +            cjkCodepoints, +            newIdxDelta, +            newIdxDelta(cjkCodepoints), +            newIdx, +            newdata, +            newdata(newIdx), +        function [u,cats], +                isMn, +                u(i).Data(isMn), +                cats{i}(isMn), +function tf, +tf, +udata, +cats, +    %   tokenizer, +    %   tokens, +    properties(Access, +            %   tokenizer, +            %   tokens, +                nvp.BasicTokenizer, +                nvp.IgnoreCase, +                this.Basic, +            this.WordPiece, +            this.Encoding, +        function toks, +            basicToks, +            toks, +                theseBasicToks, +                theseSubToks, +                for j, +                    if mod(j,2), +                        theseSubToks{j}, +                toks{i}, +        function idx, +            %   encoded, +            idx, +            %   decoded, +                args.IgnoreCase (1,1) logical, +            this.TokenizedDocumentOptions, +                txt, +            t, +        function text, +            % tokens, +            text, +        WhitespaceTokenizer, +            % tok, +                nvp.UnknownToken (1,1) string, +                nvp.MaxTokenLength (1,1) {mustBePositive,mustBeInteger}, +            this.Unk, +            this.MaxChar, +            this.Vocab, +            sub, +                token, +                    tokens, +                isBad, +                start, +                subTokens, +                    finish, +                    currentSub, +                        sub.Data, +                            sub.Data, +                        strForm, +                            currentSub, +                        finish, +                        isBad, +                    subTokens(end+1), +                    start, +        function vocab, +            if exist(vocab,'file')~, +            fid, +            c, +            words, +            empties, +            words(empties), +            vocab, +    %   sequences, +        PaddingToken, +        StartToken, +        SeparatorToken, +        MaskToken, +    properties(GetAccess, +            %    tokenizer, +            %   sequences, +                vocabFile (1,1) string {mustBeFile}, +                nvp.FullTokenizer, +                ignoreCase, +                this.FullTokenizer, +            this.PaddingCode, +            this.SeparatorCode, +            this.StartCode, +            this.MaskCode, +                text_b string, +            if ~isempty(text_b) && numel(text_a)~, +            inputShape, +            text_a, +            text_b, +                tokens_b, +                tokens, +             tokens, +        function x, +            %   seqs, +            x, +            %   x, +            %   text, +                x, +function params, +%   parameters, +paramsStructFile, +paramsStruct, +params, +function varargout, +    nvp.InputMask {mustBeNumericOrLogical}, +    nvp.DropoutProb (1,1) {mustBeNonnegative,mustBeLessThanOrEqual(nvp.DropoutProb,1),mustBeNumeric}, +    nvp.AttentionDropoutProb (1,1) {mustBeNonnegative,mustBeLessThanOrEqual(nvp.AttentionDropoutProb,1),mustBeNumeric}, +    nvp.Outputs {mustBePositive,mustBeLessThanOrEqualNumLayers(nvp.Outputs,parameters),mustBeInteger,mustBeNumeric}, +    nvp.PaddingCode (1,1) {mustBePositive,mustBeInteger,mustBeNumeric}, +    nvp.SeparatorCode (1,1) {mustBePositive,mustBeInteger,mustBeNumeric}, +nvp.Outputs, +maxLayer, +w, +hyperparameters, +    inputMask, +seq_len, +positions, +num_layers, +varargout, +    z, +    toAssign, +    varargout(toAssign), +    val, +function path, +path, +    modelName (1,1) string, +    nvp.InputMask {mustBeALogicalOrDlarrayLogical}, +[varargout{:}], +function [clf,score], +% clf, +% [clf,score], +logits, +[~,clf_i], +classes, +score, +clf, +    filePath, +localFileDir, +modelType, +modelDirectory, +function [X, present], +%   [X, present], +XNorm1, +[A, present], +X, +XNorm2, +M, +        Cache, +        TokenizationExpression, +        ByteEncoder, +            vocabFile, +            bpeData, +            bpeData(1), +            this.BPERanks, +            encoderFile, +            encoderData, +        function numericTokens, +            [inputTokens, ~], +            encodedTokens, +            bpeTokens, +            numericTokens, +                bpeTokensSplit, +                    numericTokens, +            [~,text], +        function word, +                word, +                wordFragments, +                pairs, +                    matches, +                    for i, +                        match, +                        matches, +                    minIndex, +                    bigram, +                    first, +                    second, +                    newWordFragments, +                    i, +                        j, +                            wordFragments, +                            newWordFragments, +                            i, +                        if wordFragments(i), +                                wordFragments(i+1), +                    wordFragments, +                    if numel(wordFragments), +                        pairs, +                this.Cache(token), +function cs, +%cs, +bs, +n, +for b, +    if ~any(b, +        bs, +        cs, +        n, +[~,sortedIndices], +function pairs, +numLetters, +pairIndices, +pairs, +function newParameters, +s, +newParameters, +newParameters.Hyperparameters, +newParameters.Weights, +originalWeightNames, +newWeightNames, +        nameSplit, +        newParameters.Weights.(char(nameSplit(1))).(char(join(nameSplit(2:end),'_'))), +        newParameters.Weights.(char(newWeightNames{i})), +function s, +    s, +    fn, +    for i, +        others, +        s.(fn{i}), +function [logits, presents], +%   [logits, presents], +weights, +seqLen, +h, +positionOffset, +presents, +    layerName, +    [h, present], +    presents{i}, +function sample, +%   sample, +cdf, +sample, +function logits, +%   logits, +    extractedLogits, +[~,classRanks], +notTopKIndices, +notTopKRows, +notTopKColumns, +logits( notTopKIndices ), +function [A, present], +%   [A, present], +    nvp.CausalMask (1,1) logical, +    nvp.Dropout (1,1) double {mustBeNonnegative,mustBeLessThanOrEqual(nvp.Dropout,1)}, +    nvp.InputMask, +C, +splitSize, +Q, +K, +V, +    PK, +    PV, +    K, +    V, +present, +A, +function Z, +Z, +% z, +ps, +dropoutMask, +function W, +%   W, +    numAllSubwords, +    numInputSubwords, +    numPreviousSubwords, +    mask, +    W, +function A, +%   A, +W, +normalizationDimension, +epsilon, +U, +S, +mdl, +tokenizer, +filename, +data, +data.Category, +numClasses, +data.Tokens, +cvp, +dataTrain, +dataValidation, +numObservationsTrain, +numObservationsValidation, +textDataTrain, +textDataValidation, +TTrain, +TValidation, +tokensTrain, +tokensValidation, +dsXTrain, +dsTTrain, +cdsTrain, +dsXValidation, +dsTValidation, +cdsValidation, +miniBatchSize, +paddingValue, +maxSequenceLength, +mbqTrain, +mbqValidation, +    mdl.Parameters.Weights, +featuresTrain, +    X, +    features, +    featuresTrain, +featuresValidation, +    featuresValidation, +numFeatures, +layers, +opts, +net, +YPredValidation, +accuracy, +reportsNew, +tokensNew, +XNew, +featuresNew, +labelsNew, +function X, +function Y, +    args.DropoutProbability, +dropoutProbabilitiy, +Y, +outputSize, +mdl.Parameters.Weights.classifier.kernel, +mdl.Parameters.Weights.classifier.bias, +numEpochs, +learnRate, +lineLossTrain, +trailingAvg, +trailingAvgSq, +parameters, +iteration, +start, +for epoch, +        iteration, +        [X,T], +        [loss,gradients], +        [parameters.Weights,trailingAvg,trailingAvgSq], +        loss, +        D, +dsXNew, +mbqNew, +YPredNew, +function [X,T], +T, +bias, +function [loss,gradients], +dropout, +loss, +gradients, +function predictions, +predictions, +    dlX, +    dlYPred, +    YPred, +    predictions, +data.Properties.VariableNames, +str, +strMasked, +sentencePred, +tokens, +idx, +tokens{1}(idx), +scores, +[~,idxTop], +tbl, +tbl.Token, +tbl.Score, +[![CircleCI](https://img.shields.io/circleci/build/github/matlab-deep-learning/transformer-models?label, +[![Open in MATLAB Online](https://www.mathworks.com/images/responsive/global/open-in-matlab-online.svg)](https://matlab.mathworks.com/open/github/v1?repo, +`mdl, +`Z, +`sentiment, +`[sentiment, scores], +`summary, +[sentiment,scores], +inputText, +summary, +function mdl, +        "japanese-base-wwm"])}, +        mdl, +        params, +        ignoreCase, +        vocabFile, +        params.Hyperparameters, +zipFilePath, +modelDir, +btok, +vocabFile, +ftok, +tok, +params.Weights, +params.Hyperparameters, +    NumHeads, +    NumLayers, +    NumContext, +    HiddenSize, +    Tokenizer, +    Parameters, +        "sentiment-model"])}, +ignoreCase, +function summary, +%   summary, +    nameValueArguments.MaxSummaryLength (1,1) {mustBeInteger, mustBePositive}, +    nameValueArguments.TopK             (1,1) {mustBeInteger, mustBePositive}, +    nameValueArguments.Temperature      (1,1) {mustBePositive}, +    nameValueArguments.StopCharacter          {iIsScalarString}, +maxSummaryLength, +topK, +temperature, +stopCharacter, +enc, +inputTokens, +    inputTokens, +    inputText, +pasts, +[~, presents], +previousToken, +    [logits, presents], +    logits, +    probabilities, +    nextToken, +    previousToken, +    textToPrint, +    summary, +    if textToPrint, +mdl.Tokenizer, +mdl.Parameters, +function out, +%   out, +inSize, +[seqs,pieces], +maskedProbs, +[~,sampleIdx], +predictedTokens, +out, +numMaskPerSeq, +maskStartIdx, +    startIdx, +    maskStartIdx, +    out(i), +    out, +        inferTypeID, +            sep, +            act, +            exp, +            pad, +            x1, +            type1, +            x2, +            type2, +classdef(SharedTestFixtures, +        FunctionUnderTest, +            mdl, +            probs, +            txt, +            numObs, +            params, +            seqs, +            paddingValue, +            seqsPadded, +            vocabularySize, +            z, +            str, +            seq, +            toMask, +            x(toMask), +            k, +            maskProbabilities, +            [~, topk], +            actualProbSum, +            expectedProbSum, +            tolerance, +function constraint, +constraint, +        InvalidOutputs, +        InvalidCodes, +        InvalidDropoutProb, +        InvalidInputMask, +        InvalidInputX, +        InvalidModelParams, +        InvalidModelName, +            v1, +            vend, +            vmiddle, +            z1, +            zend, +            zmiddle, +            tol, +            y1, +            y2, +            y, +            txt_a, +            txt_b, +            z2, +            seqlen, +            batchsize, +            outputs, +            [z1,z3,z5], +            expSize, +            mdl.Parameters.Hyperparameters.NumLayers, +            z5_exp, +            [z1,z2,z3,z4,~,z6], +            z_exp, +            strs, +            [x,xmask], +            y_default, +            y_inputmask, +            nomask, +            y_nomask, +function ids, +ids, +            exp_out, +            act_out, +            manyStrs, +            aControlChar, +            aFormatChar, +            aSpaceChar, +            act_toks, +            exp_toks, +            makeTok, +            [mock,behaviour], +            doc, +            toksExp, +            customToken, +        ModelAndExpectedVocabSize, +            enc, +            fixture, +            folder, +            testVocab, +            tokFromEncoding, +            tokFromFile, +            unk, +            ustr, +            maxLen, +            wsTok, +        VocabFiles, +        Constructor, +            tok2, +            act_y, +            exp_y, +                exp_y(i), +            act_decode, +            exp_decode, +            xLower, +            xUpper, +            yLower, +            yUpper, +            isNotEqualTo_yUpper, +function vocabFiles, +versions, +vocabFiles, +    vocabFiles{i}, +            expectedEncoding, +function modelNames, +modelNames, +    versionName, +    vocabDir, +    model, +    vocabFiles.(replace(model, "-", "_")), +function japaneseBERTTokenizer, +japaneseBERTTokenizer, +            [~,topk], +            lmFunction, +            [sentClass,sentScore], +    properties(Constant, Access, +        block, +        Input, +            C, +            weights, +            hyperParameters.NumHeads, +            K_fake, +            V_fake, +            past, +            [y,present], +            exp_present_size, +            exp_present_size(2), +        function weights, +            g1, +            b1, +            g2, +            b2, +            W_A1, +            W_A2, +            b_A1, +            b_A2, +            W_P1, +            b_P1, +            W_P2, +            b_P2, +        function s, +            s_ln, +            s_attn, +            s_mlp, +            fn, +            fv, +            s, +                s.(fn{i}), +        ExpectedFiles, +            aFile, +            directory, +            files, +            filenames, +        ExpectedNumHeads, +        ExpectedNumLayers, +        ExpectedContext, +            test.Parameters, +            expected, +            hyperParams, +            w, +        model, +        InputData, +            X, +            [pasts, parameters], +            seqLen, +            vocabSize, +            Ybatch, +            YperObs, +                YperObs(:, :, i), +        function [pasts, parameters], +            parameters, +            pasts, +        function pasts, +        function parameters, +            parametersFile, +        UnicodeTextAndEncodedText, +        StringToTokenize, +        StringToEncode, +            inputText, +            expectedOutputText, +            actualOutputText, +            ch, +            firstLine, +            splitfirstLine, +            actBPE, +            actBPEMatchesComment, +            encoded, +            decoded, +            test.Encoder, +function modelName, +function modelPath, +modelPath, +function parameter, +parameter, +parameter.NormalSentenceFragment1, +parameter.NonsenseFragment1, +% correctly. If you set "k, +parameter.NonBreakingSpace, +function c, +c, +function toks, +[toks,~], +            numClasses, +            class, +            probabilities, +            probabilities(class), +            sample, +            numSamples, +            uniformProbabilities, +            samples, +            counts, +            expMean, +            % When K, +            x(class), +            % Call topKLogits with K, +            topK, +            logits, +            actProb, +            expProb, +            expProb(class), +            K, +            classes, +            x(classes), +            expProb(classes), +            yAct, +            yExp, +        UncasedVersion, +        AllModels, +            mdlDefault, +            text_b_next, +            text_b_random, +            prediction, +            probability, +classdef (SharedTestFixtures, +        Models, +            dirs, +            dataDirsExist, +            this.DataDirExists, +                model, +        function path, +            modelDir, +            path, +        function modelDirs, +            modelDirs, +        GPT2DataDir, +        GPT2DataFiles, +            dataFileExists, +            this.DataFileExists, +            for file, +            modelNames, +thisFile, +thisDir, +        ValidText, +            inSize, +            outputText, +function validText, +manyStrs, +singleStr, +validText, +        attention, +        multiheadAttention, +        NumQueries, +        NumObs, +            latentDim, +            hyperParams.NumHeads, +            [q,k,v], +            [Q,K,V], +            [~,past], +            [~,K,V], +            [yAct,present], +            pastK, +            pastV, +            V, +            numQueries, +            default, +            masked, +            p, +            yDefault, +            yNoDropout, +            inputDim, +            W1, +            outputDim, +            W2, +function [Q,K,V], +        convolution1d, +            W, +            b, +            z_act, +        Size, +        Probability, +            actMean, +            f, +            function [val,df_val], +                val, +                df_val, +            [f_val,df_val], +        gelu, +        Tolerance, +        Value, +        Cast, +            actual, +            constraint, +            num, +            casted, +        function tol, +            absTol, +            relTol, +        maskAttentionWeights, +            act_masked, +            [m,n], +            mask, +            masked_vals, +        Activation, +        multiLayerPerceptron, +        function z, +        Dim, +            keyDim, +            numKeys, +            q, +            v, +            attnAct, +            attnExp, +            valDim, +            attnProbs, +            Q, +            % Use V, +            attnScores, +                attnExp, +            numHeads, +            for h, +                attnScores, +                for i, +                    maskedAttnScores, +                    probs, +                    attnExp(:,numQueries-i,h), +            batchSize, +            A, +            attnNoDropout, +            attnWithDropout, +            A_default, +            A_exp, +            for n, +                attnExp(:,:,:,n), +        function w, +        function a, +            a, +        normalization, +        Eps, +        Precision, +        InputClass, +                y, +mu, +sig, +xhat, +%   tokenizer, +%   sequences, +%   truncatedSequences, +    nvp.SeparatorCode (1,1) double {mustBeInteger}, +if idx, +maxSeqLen, +indices, +z1, +z2, +n1, +n2, +N, +    delta, +        n1, +            gap, +            n1, +            n2, +        n2, +    z1, +    z2
- **函数变更**: parses, handles, is, ., mustBeFileOrEncoding(x), mustBeLessThanOrEqualNumLayers(x,params), mustBeALogicalOrDlarrayLogical(val), mustBeNumericDlarray(val), iCreateDirectoryIfItDoesNotExist(directory), iDownloadFileIfItDoesNotExist(destination,, does, ality., returns, expects, download(), ,, )., computes, and, truncates, to, maps, s., preprocess, |model|, performs, makes, determines, takes, iIsScalarString(s), oneObsOneSeparator(test), oneObsOneSeparatorWithPadding(test), oneObsTwoSeparators(test), oneObsTwoSeparatorsEdgeCase(test), oneObsTwoSeparatorsPadded(test), batchedCase(test), canBatch(test), verifyOutputDimSizes(test), canPredictMaskedTokens(test), checkProbDistrOverChannelDimBatches(test), matchesExpectedValue(test), canEncodeSentencePairs(test), checkDuplicateSentence(test), defaultOutputsIsLastLayer(test), outputsCanBeUsed(test), outputsCanDuplicateAndBeOutOfOrder(test), negativeTestOutputs(test,InvalidOutputs), negativeTestSeparatorCode(test,, negativeTestPaddingCode(test,, negativeTestDropoutProb(test,, negativeTestAttentionDropoutProb(test,, negativeInputMask(test,, negativeInputX(test,, negativeModelParameters(test,, errorsForIncorrectModelName(test,, canUseInputMask(test), canConstruct(test), canTokenize(test), canTokenizeBatch(test), removesControlCharactersAndWhitespace(test), splitsOnNewlines(test), tokenizesCJK(test), splitsOnPunctuation(test), stripsAccents(test), canBeCaseSensitive(test), matchesExpectedTokenization(test), errorsIfBasicTokenizerIsNotTokenizer(test), canSetBasicTokenizer(test), tokenizationMatchesTokenizedDocument(test), canSetOptions(test), canConstructWithFile(test), canSetUnknownToken(test), canSetMaxTokenLength(test), hasExpectedVocabSize(test,ModelAndExpectedVocabSize), canConstructWithNonDefaultModel(test), hasExpectedProperties(test,, canEncodeOneSentence(test), canEncodeMultipleSentences(test,, canEncodeSentencePair(test,, canEncodeMultipleSentencePairs(test,, errorsForDifferentNumberOfSentencePairObservations(test,, canDecode(test), canDecodeMultipleObservations(test,, canDecodePaddedBatch(test,, canIgnoreCase(test,, checkCaseSensitivity(test,, matchesExpectedEncoding(test,, languageModelErrorsWithSAParams(test), canUseSentimentAnalysisModel(test), outputHasInputSize(test,Input), outputHasInputSizeWithPasts(test,Input), verifyFilesExist(test), loadParameters(test), verifyLoadStructFields(test), verifyHyperparameters(test), verifyWeights(test), canUseModel(test,, canAcceptBatches(test), setup(test), encodeGivesCorrectResults(test,, decodeGivesCorrectResults(test,, testTokenization(test,, byteEncoderSize(test), independenceOfInputClass(test,, commentNotInBPE(test), decodeInvertsEncode(test,StringToEncode), setupEncoder(test), canSampleDeterministic(test), canSampleUniform(test), testForKIsOne(test), testForLargeK(test), dlarrayIsSupported(test), canConstructModelWithDefault(test), canConstructAllModels(test,, canConstructModelWithNVPAndVerifyDefault(test), checkBertIgnoreCase(test,, multicasedVersionIsCaseSensitive(test), canDoNSP(test), canConstructModelWithNVP(test), canConstructLanguageModel(test), setup(this), teardown(this), verifyOutputDimSizes(test,, maskTokenIsRemoved(test,, inputWithoutMASKRemainsTheSame(test,, has, checkSingleHeadNoFullyConnected(test,NumQueries,NumObs), checkMultiHeadNoFullyConnected(test,NumQueries,NumObs), checkPastPresentCaching(test,NumQueries,NumObs), checkInputOutputFC(test,NumQueries,NumObs), defaultIsMaksed(test), canTurnOffMask(test), with, canDropout(test), defaultIsNoDropout(test), hasExpectedValue(test,Input), resetGlobalSeed(~), doesDropout(test,Size,Probability), isRandom(test), supportsDlarrayAndAutodiff(test), matchesExpectedValue(test,Value), supportsMultipleTypes(test,Cast), returnsValueWhenOneKey(test), isScaledDotProduct(test), isExpectedValue(test,Dim), multipleQueries(test), multipleHeads(test), defaultIsMasked(test), multipleObservations(test), verifyDlarrayEqual(test,x,y,varargin), computesExpectedValue(test,Precision,InputClass,Value)

---

