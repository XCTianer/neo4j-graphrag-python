# Neo4j浏览器图形显示使用说明

## 概述
本文档说明如何在Neo4j浏览器中显示代码变更的完整影响链路图形。

## 文件说明

### 1. `single_graph_display.cypher` - 推荐使用
**最简单的查询，在一张图中显示所有影响链路**

```cypher
MATCH (source)-[r:USES|DEFINES|CALLS]->(target)
WHERE source.file_path = '+gpt2/model.m' OR target.file_path = '+gpt2/model.m'
RETURN source, r, target
```

**特点：**
- 显示所有与变更文件相关的实体和关系
- 包含内部依赖和外部依赖
- 图形清晰，易于理解
- 适合快速查看整体影响网络

### 2. `complete_impact_chains_graph.cypher` - 详细分析
**包含多种显示方法的完整查询集合**

包含4种不同的显示方法：
- 方法1：完整网络显示
- 方法2：分层显示（更清晰）
- 方法3：按风险等级显示
- 方法4：最全面的显示

### 3. `graph_visualization_queries.cypher` - 多种视角
**提供10种不同的查询视角**

- 查询1：核心网络
- 查询2：presents变量影响
- 查询3：gpt2.model函数依赖
- 查询4：高风险链路
- 查询5：完整依赖网络
- 查询6：按类型分组
- 查询7：层次结构
- 查询8：变量依赖
- 查询9：函数调用链
- 查询10：完整影响网络

## 使用步骤

### 1. 打开Neo4j浏览器
```bash
# 在浏览器中访问
http://localhost:7474
```

### 2. 登录Neo4j
- 用户名：neo4j
- 密码：password

### 3. 运行查询
1. 复制查询语句到Neo4j浏览器的查询框
2. 点击"Run"按钮
3. 查看图形结果

### 4. 图形操作
- **缩放**：使用鼠标滚轮
- **拖拽**：点击并拖拽节点
- **选择**：点击节点或关系
- **布局**：使用右上角的布局选项

## 推荐使用顺序

### 第一步：整体概览
使用 `single_graph_display.cypher` 查看整体影响网络

### 第二步：重点关注
使用 `graph_visualization_queries.cypher` 中的查询2查看presents变量的影响

### 第三步：详细分析
使用 `complete_impact_chains_graph.cypher` 中的方法2进行分层分析

## 图形解读

### 节点颜色
- **蓝色**：函数（Function）
- **绿色**：变量（Variable）
- **黄色**：脚本（Script）
- **灰色**：其他实体

### 关系类型
- **USES**：使用关系（实线箭头）
- **DEFINES**：定义关系（虚线箭头）
- **CALLS**：调用关系（粗线箭头）

### 影响链路
- **红色箭头**：高风险影响
- **黄色箭头**：中风险影响
- **绿色箭头**：低风险影响

## 注意事项

1. **性能考虑**：如果图形过于复杂，可以限制查询结果数量
2. **浏览器兼容性**：建议使用Chrome或Firefox
3. **内存使用**：大型图形可能占用较多内存
4. **导出功能**：可以使用Neo4j浏览器的导出功能保存图形

## 故障排除

### 问题1：图形不显示
- 检查查询语法是否正确
- 确认Neo4j连接正常
- 检查是否有数据返回

### 问题2：图形过于复杂
- 使用更具体的查询条件
- 限制关系类型
- 减少路径长度

### 问题3：性能问题
- 添加LIMIT子句
- 使用更精确的匹配条件
- 分批查询

## 示例结果

运行 `single_graph_display.cypher` 后，你应该能看到：

1. **中心节点**：`+gpt2/model.m` 文件中的所有实体
2. **内部关系**：文件内实体之间的依赖关系
3. **外部关系**：与外部文件的依赖关系
4. **影响链路**：从presents变量开始的所有影响路径

这样就能在一张图中完整地看到所有60条影响链路的网络结构！ 